package ch.ge.cti_composant.gitSync.common

import spock.lang.Specification
import spock.lang.Unroll

/**
 * Tests class {@link ch.ge.cti_composant.gitSync.util.LDAP.LDAPGroup}.
 */
@Unroll
class GitSyncSpec extends Specification {

    /*
    def "#buildGitlabContext should create a correct GitLab tree"() {
        given:
        def ldapTree = new DataProvider().setupLdapTree()

        when:
        def gitlab = GitSync.buildGitlabContext("hostname", "apiToken", ldapTree)

        then:
        gitlab.
    }
    */

}
